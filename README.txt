运行runme.py开始监听
用记事本编辑runme.py
	#规则开始 处是开始编辑规则的地方，调用方法如下：
	grouprules(群号，保存实时消息的变量，检测到什么消息 可以接变量，检测到后发送，端口)
	privaterules(qq号，保存实时消息的变量，检测到什么消息 可以接变量，检测到后发送，端口)
	所有规则放rule.py里 不要和rules.py弄混，其他文件按需求修改
	ok完事
			6:39 2023/10/15 Def_main